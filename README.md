# Кафе Соня - Сайт кондитерской

React приложение для сайта кондитерской "Кафе Соня" с возможностью просмотра информации о тортах, их рецептах и истории кафе.

## 🚀 Технологии

- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Webpack 5** - сборка проекта
- **Express** - backend API сервер
- **ESLint** - проверка кода
- **CSS3** - стилизация с поддержкой темной темы

## 📋 Возможности

- ✨ Просмотр информации о тортах (Наполеон, Медовик, Чизкейк)
- 📖 Детальные рецепты с ингредиентами
- 🌓 Переключение между светлой и темной темой
- 📱 Адаптивный дизайн
- 🔄 API для получения данных о тортах
- 📝 Страницы: Главная, Тортики, Контакты, История

## 🛠️ Установка

```bash
# Установка зависимостей
npm install
```

## 🏃 Запуск проекта

### Разработка

Запуск сервера и dev server одновременно:
```bash
npm run dev
```

Или отдельно:
```bash
# В одном терминале - Express сервер (порт 3001)
npm run server

# В другом терминале - Webpack dev server (порт 3000)
npm start
```

### Production сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## 📁 Структура проекта

```
websiteCafe/
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   │   ├── Header.jsx
│   │   └── Footer.jsx
│   ├── contexts/        # React контексты
│   │   └── ThemeContext.jsx
│   ├── pages/          # Страницы приложения
│   │   ├── Home.jsx
│   │   ├── Cakes.jsx
│   │   ├── Contacts.jsx
│   │   ├── History.jsx
│   │   ├── Napoleon.jsx
│   │   ├── Medovik.jsx
│   │   └── Cheesecake.jsx
│   ├── App.jsx         # Главный компонент
│   ├── index.js        # Точка входа
│   └── styles.css      # Стили
├── public/             # Статические файлы
├── server.js           # Express API сервер
├── webpack.config.js   # Конфигурация Webpack
└── package.json
```

## 🔌 API Endpoints

- `GET /cakes` - получить список всех тортов
- `GET /cakes/:id` - получить информацию о конкретном торте

## 📜 Скрипты

- `npm start` - запуск webpack dev server
- `npm run server` - запуск Express сервера
- `npm run dev` - запуск сервера и dev server одновременно
- `npm run build` - сборка production версии
- `npm run lint` - проверка кода ESLint
- `npm run lint:fix` - автоматическое исправление ошибок ESLint

## 🎨 Темы

Приложение поддерживает две темы:
- **Светлая тема** (по умолчанию)
- **Темная тема**

Переключение темы доступно через кнопку в header. Выбор темы сохраняется в localStorage.

## 📝 Лицензия

ISC
